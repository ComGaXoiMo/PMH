const rules = {
  projectId: [{ required: true }],
  unitId: [{ required: true }],
  userId: [{ required: true }],
  userGroupId: [{ required: true }],
  feedbackTypeId: [{ required: true }]
}

export default rules
